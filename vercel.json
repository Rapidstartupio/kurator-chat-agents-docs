{
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "build": {
          "env": {
            "PATH": "/usr/local/bin:$PATH"
          }
        },
        "outputDirectory": "."
      }
    }
  ]
}
